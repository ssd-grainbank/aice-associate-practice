# 8주 합격 커리큘럼

기준 학습량은 주 5일, 하루 45~60분입니다. 이미 파이썬 경험이 있으면 1~2주차를 진단점수 85% 이상일 때
건너뛸 수 있습니다. 시험일이 급하면 1주에 두 주차를 묶어 4주 압축 과정으로 사용합니다.

## 매일 동일한 50분 루프

1. 5분: 만기 오답을 설명 없이 다시 푼다.
2. 10분: 오늘 개념 하나를 AI 코치와 대화한다.
3. 25분: 노트북에서 코드를 직접 작성한다.
4. 10분: 대시보드 문제를 풀고 틀린 이유를 한 문장으로 적는다.

## 1주차 — 파이썬 생존 문법

- 변수, 숫자·문자열·불리언, 리스트·딕셔너리
- 인덱싱과 슬라이싱, 조건문, 반복문, 함수 호출
- Jupyter 셀 실행, `NameError`·`TypeError`·`KeyError` 읽기
- 통과 기준: 짧은 코드의 출력을 실행 전에 예측하고 간단한 오류를 스스로 고친다.

## 2주차 — pandas 데이터 분석

- `read_csv`, `head`, `shape`, `info`, `describe`
- 열 선택, 조건 필터, 정렬, `value_counts`, `groupby`
- 수치형·범주형 구분과 목표변수 확인
- 통과 기준: 처음 보는 CSV의 상태를 5분 안에 요약한다.

## 3주차 — 품질 점검과 결측·이상치

- 결측 개수·비율, 중복, 자료형 오류
- `dropna`, `fillna`, 중앙값·최빈값 대치
- 사분위수와 IQR, 이상치 제거 전 도메인 판단
- 통과 기준: 전처리 선택의 이유와 전후 행 수를 설명한다.

## 4주차 — 모델 입력 만들기

- 명목형과 순서형, one-hot과 label/ordinal encoding
- StandardScaler와 MinMaxScaler
- `train_test_split`, `random_state`, `stratify`
- 데이터 누수 방지: 전처리 `fit`은 학습 데이터에만
- 통과 기준: 원본 데이터에서 `X_train`, `X_test`, `y_train`, `y_test`를 안전하게 만든다.

## 5주차 — 머신러닝 모델

- 분류·회귀 판별
- 로지스틱 회귀, 결정트리, 랜덤포레스트, KNN, 선형회귀
- `fit`, `predict`, baseline 비교
- 통과 기준: 문제 유형에 맞는 모델을 선택하고 전체 학습 코드를 빈 화면에서 작성한다.

## 6주차 — 성능 평가와 개선

- 혼동행렬, accuracy, precision, recall, F1
- MAE, MSE, RMSE, R²
- 과적합·과소적합, 교차검증, 하이퍼파라미터 탐색
- 특성 중요도와 인과관계의 차이
- 통과 기준: 점수 하나가 아니라 업무 비용에 맞는 지표를 고른다.

## 7주차 — 딥러닝과 시각화

- Dense 층, ReLU, sigmoid, softmax, 손실 함수
- epoch, batch size, validation, EarlyStopping
- 학습·검증 loss 그래프 해석
- 통과 기준: 이진 분류·다중 분류·회귀의 출력층을 구분한다.

## 8주차 — 90분 실전

- 분석 20분 → 전처리 25분 → 모델링 35분 → 검산 10분
- 막힌 셀에서 3분 이상 쓰지 않고 다음 문제로 이동
- 오픈북 검색용 개인 코드 색인 1장 작성
- 모의시험 85점 이상 3회 연속이면 응시

## 실패했을 때 조정 규칙

- 문법 오류가 많다: 1주차 전체 복습 대신 그 오류가 난 코드 3개만 다시 작성합니다.
- 함수 이름이 기억나지 않는다: 외우지 말고 작업별 코드 색인을 만듭니다.
- 개념 문제는 맞는데 코딩이 막힌다: 답을 보고 닫은 뒤 빈 셀에서 재작성합니다.
- 시간이 부족하다: 완벽한 모델보다 데이터 확인·전처리·기본 모델 점수를 먼저 확보합니다.
